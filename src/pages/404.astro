---
import Base from "../layouts/Base.astro";
---

<Base 
  title="404 — Fréquence introuvable — RadioABF"
  description="Page non trouvée sur RadioABF. Retourne à l'accueil ou explore les autres sections."
>
  <section class="min-h-[70vh] flex items-center justify-center px-4 sm:px-6">
    <div class="text-center max-w-2xl">
      <!-- Titre principal -->
      <h1 class="text-6xl sm:text-8xl font-extrabold tracking-tight">
        <span class="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">
          404
        </span>
      </h1>
      <p class="mt-6 text-2xl sm:text-3xl font-bold text-white/90">
        Oups… Fréquence introuvable !
      </p>
      <p class="mt-4 text-lg text-white/70 leading-relaxed">
        La page que tu cherches semble avoir quitté l’antenne.<br />
        Peut-être qu’elle mixe ailleurs dans l’univers ?
      </p>

      <!-- Boutons d’action -->
      <div class="mt-10 flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          href="/"
          class="inline-flex items-center justify-center px-8 py-4 rounded-2xl font-semibold text-lg
                 bg-gradient-to-r from-[#145C8F] to-[#2FC2FF]
                 shadow-lg shadow-cyan-500/30 hover:scale-[1.02] transition"
        >
          ← Retour à l’accueil
        </a>
        <a
          href="/music"
          class="inline-flex items-center justify-center px-8 py-4 rounded-2xl font-semibold text-lg
                 border border-white/20 bg-white/5 hover:bg-white/10 transition"
        >
          Aller sur Music & charts
        </a>
      </div>

      <!-- Petit clin d’œil fun -->
      <div class="mt-16 text-white/50 text-sm">
        <p>💡 Astuce : essaie de chercher dans les programmes ou les news.</p>
        <p class="mt-2">Ou lance le player, il y a toujours de la bonne musique en direct ! 🎶</p>
      </div>
    </div>
  </section>
</Base>